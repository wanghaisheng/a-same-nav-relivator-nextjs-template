支付接入推荐做法:
1.webhook接收支付状态通知,处理订单状态变更,需要做好幂等处理,也就是基于订单号和状态去处理,即使被多次调用,也应该能够正确处理,而不是调用一次就加一次积分;
2.回调跳转用于告诉用户最终处理状态,这里不能相信前端数据,而应该根据订单号从后端拿订单状态,显示给用户。
哥飞
20:28:04
这两个,都应该支持多次调用而不会多次加积分。
哥飞
20:30:01
另外还要注意,检查订单中记录的金额跟回调给你的金额是否一致,如果不一致,就是用户恶意篡改金额,需要记为异常订单。
否则就会出现1分钱买任意套餐。
即刻@哥飞